# infra/docker

Entorno de desarrollo local: `docker-compose.yml` con PostgreSQL, Redis
y las apps `api`/`web` en modo desarrollo (hot reload).

Ver `docs/DEPLOYMENT.md` §2.

**Estado**: pendiente de implementación (Fase 10 del roadmap, aunque el
`docker-compose.yml` de desarrollo puede adelantarse en la Fase 4 al
iniciar el backend).
